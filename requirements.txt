# Core ML libraries
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0
scipy>=1.7.0

# Deep learning
torch>=1.9.0
torchvision>=0.10.0
# tensorflow>=2.6.0  # Commented out as it's not compatible with current Python version

# Data visualization
matplotlib>=3.4.0
seaborn>=0.11.0
plotly>=5.0.0

# ECG-specific libraries
# biosppy>=0.8.0  # Commented out as it might cause dependency issues
# heartpy>=1.2.7  # Commented out as it might cause dependency issues
# wfdb>=3.4.0  # Commented out as it might cause dependency issues
# pyecg>=0.1.0  # Commented out as it might cause dependency issues

# Data augmentation
# albumentations>=1.1.0  # Commented out as it might cause dependency issues
# tsaug>=0.2.1  # Commented out as it might cause dependency issues

# Signal processing
pywavelets>=1.1.1

# Jupyter and development
jupyter>=1.0.0
ipykernel>=6.0.0
tqdm>=4.62.0

# Model evaluation and metrics
imbalanced-learn>=0.8.0

# Data compression and serialization
h5py>=3.3.0
joblib>=1.0.0 